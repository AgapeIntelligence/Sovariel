[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "reflexive-lattice"
version = "0.1.0"
description = "Coherence and bounded-entropy monitor for alignment research"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name = "Evie (@3vi3Aetheris)"}]
dependencies = ["numpy>=1.21"]

[project.urls]
Repository = "https://github.com/AgapeIntelligence/Sovariel"
